import VolumeBlock from "./volumeBlock/VolumeBlock";

const PlayerOthers = () => {
   return (
      <div className="playerOthers w-full h-full flex items-center">
         <VolumeBlock />
      </div>
   );
};

export default PlayerOthers;
