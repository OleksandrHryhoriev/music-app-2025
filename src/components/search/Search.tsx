import SearchForm from "./SearchForm";
import SearchWrapper from "./SearchWrapper";

const Search = () => {
   return (
      <SearchWrapper>
         <SearchForm />
      </SearchWrapper>
   );
};

export default Search;
